# Node / pnpm
node_modules/
dist/
*.tsbuildinfo
.pnpm-store/

# Local-only bundled-serving test build (vite build --outDir ../backend/web).
# In the image the frontend is copied to /app/web at build time, never committed.
/backend/web/

# Go
*.exe
*.test
*.out
coverage.out
results.sarif
/backend/data/*.db
/backend/data/*.sqlite

# Local per Agency SQLite databases (created by start.sh in backend/)
/backend/*.db
/backend/*.db-journal
/backend/*.db-wal
/backend/*.db-shm
/backend/*.sqlite
/backend/*.sqlite3

# Local env
.env
.env.local
.env.*.local
.env

# Per Agency branding configs are deployment-specific; only the default template is tracked.
/frontend/public/configs/*.branding.json
!/frontend/public/configs/default.branding.json

# IDE / OS
.DS_Store
.vscode/
.idea/
*.swp
